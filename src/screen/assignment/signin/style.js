import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 24,
    },

    button: {
        marginVertical: 20,
    },

    titleText: {
        fontWeight: 'bold',
        color: 'black',
        fontSize: 20,
        textAlign: 'center'
    },

    footerText: {
        color: 'blue',
        textAlign: 'center',
    },

    footerLink: {
        fontWeight: 'bold',
    },

    img: {
        width: 350,
        height: 200,
        marginBottom: 20,
    }
});