import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: 'blue',
        paddingVertical: 20,
        paddingHorizontal: 8,
        borderRadius: 8,
    },

    textContent: {
        color: 'white',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold',
    },
});